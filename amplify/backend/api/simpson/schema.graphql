type User @model {
  id: ID!
  email: AWSEmail!
}

type GroceryList @model @auth(rules: [{allow: owner}]) {
  id: ID!
  title: String!
  groceries: [GroceryItem] @connection(name: "GroceryItems")
}

type GroceryItem @model {
  id: ID!
  content: String!
  quantity: String
  unit: String
  groceryList: GroceryList! @connection(name: "GroceryItems")
}
